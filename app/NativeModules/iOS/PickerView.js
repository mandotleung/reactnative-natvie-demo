/* @flow */

import { requireNativeComponent } from 'react-native';

// requireNativeComponent automatically resolves this to "RNTMapManager"
module.exports.DemoPickerView = requireNativeComponent('RNTPickerView', null);
